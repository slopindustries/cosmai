# P0-B P1 Entry Gate

- Status: `DRAFT`
- Governing decisions: [DP-001](../decisions/DP-001-p0-lifecycle.md), [DP-005](../decisions/DP-005-two-part-pre-p1-execution.md)
- P0-B experiment: [EXP-003](../../experiments/integrated-p0/EXP-003-capability-layer.md)
- Reviewed P0 revision: `9547f3b8c7a3e135f4084d70282aa9243f67c93f` — the revision this gate's
  evidence was read against, including the six P1 decision packets (DP-029…DP-034) this gate is
  itself deciding whether to accept. `[확인 사실]` The proposed `p0-archive` tag below points at a
  different, earlier revision — see the P0 isolation checks for why.
- Review date and timezone: 2026-08-21, `Asia/Seoul`
- Reviewers: project owner (acceptance); Claude session (preparation, this record); adversarial review (attack — report link to be added by Task 13)

## Gate question

Has P0-B produced enough accepted evidence, contract, and disposition information to reconstruct P1 without promoting P0 implementation code or silently carrying an unresolved blocker?

## Required outputs

| Output | Status | Evidence or link | Blocking limitation |
|---|---|---|---|
| P0 Charter exit-criteria review | `PASS` | [`p0-charter.md`](../p0-charter.md) §P0-B exit criteria; [Architecture Synthesis](architecture-synthesis-v0.1.md) Part 6; [DP-027](../decisions/DP-027-dataset-standard-and-share-alike.md); [`B4-SCENARIO-COVERAGE.md`](../../experiments/integrated-p0/evidence/B4-SCENARIO-COVERAGE.md); the annex below | `[측정]` Of the 12 charter criteria in the annex below: 4 are met cleanly with no qualification (rows 2, 3, 6, 11); 3 are met with a stated limitation (rows 1, 5, 9); 1 is met for tampering but only narrowly for evolution (row 7); 1 is partially met (row 4 — no test covers the overlap between two captures of a moving source); 1 is met only over an empty lineage and not falsifiably by its own coexistence assertion (row 8); 1 is met for `SEC-001…004` as scenarios while `SEC-006` is waived, not passed (row 10); and 1 remains pending this gate's own acceptance (row 12), true only once the owner sets an Outcome in the Decision block below, not by prior evidence. `[추론]` A `GO` outcome therefore knowingly accepts four rows whose own Status column reads as something other than a plain "met" — row 4 (**Partially met**), row 8 (**Met — over an empty lineage, and not falsifiably by this test's own "no UPDATE path" assertion**), row 10 (**Met for `SEC-001…004` as scenarios; `SEC-006` waived, not passed**), and row 12 (**Pending this gate**) — and four further rows the annex shows as met only with a named limitation it carries forward rather than smooths over: rows 1, 5, 7, and 9 (`[확인 사실]` corrected 2026-08-21, round 3, D7: an earlier revision of this sentence said "four criteria... not unconditionally met," which read literally would also sweep in rows 1, 5, 7, and 9 — met, but not without a stated limitation — and did not distinguish them from rows 4/8/10/12's stronger, non-"met" statuses). See the annex for the per-criterion detail, including six document-staleness corrections this gate makes (an earlier revision of this cell said two) — Architecture Synthesis Part 6's rows 1 and 7, Part 2's rows 3, 4, and 6, and `B4-SCENARIO-COVERAGE.md`'s "no real dataset exists" line — see the annex's own introduction for the full list. `[확인 사실]` Corrected 2026-08-21 (fix-wave round 2, N1; round 3, D1): the previous revision of this cell said "5 are met cleanly... (rows 2, 3, 6, 8, 11)," which double-counted row 8 as clean after this same commit's own F11 repair (round 1) had already qualified row 8's Status. Re-counted directly from the annex table: the bare `**Met.**` status (no further qualifying clause on the same bold span) appears in rows 2, 3, 6, and 11 only — four rows. Round 2's own correction quoted a `grep -c` command as the count's evidence; that command matches this very sentence's own description of itself, so it returns 5 against the file, not 4 — a self-matching command is dropped here rather than repeated, and the enumerated rows are the evidence instead. |
| Architecture Synthesis | `ACCEPTED (this gate, 2026-08-21)` | [`architecture-synthesis-v0.1.md`](architecture-synthesis-v0.1.md) (Status `DRAFT_FOR_GATE` entering this review) | Accepting this document as the gate's evidence base is not accepting every finding in it as a clean pass, and not accepting every summary row in it as current: Part 2 records one hypothesis refuted in its strong form (Question 8); `[확인 사실]` Part 2's own snapshot-evolution row (`:277`) and Part 6's own row 7 (`:362`) are themselves stale, not a live "half-validated" or "unproved" finding — the same document's Part 1 Question 5 (`:149`) already says the opposite as of 2026-08-20: *"It is exercised and it discriminates,"* narrower than the strong form, not unexercised; and `[확인 사실]` Part 2's rule-baseline row (`:279`) reads, quoted here exactly as the document states it and not as current: *"**Not tested.** No quality baseline was built; the normalizers extract, they do not judge."* `project-state.md` §5 records the opposite as of 2026-08-20 — a baseline was built and survived three independent attacks, on fixtures rather than real data, which is the distinction row `:279` should carry and does not. Part 6's own exit-criteria row 1 is stale against the same document's Part 1 Question 2 — see the annex below for the correction this gate makes, and for the four further staleness corrections named there. |
| `PoC Contract 0.1` | `ACCEPTED (this gate, 2026-08-21)` | [`POC-CONTRACT-0.1.md`](../../contracts/experimental/POC-CONTRACT-0.1.md) (`Last updated: 2026-08-19T+09:00`, `Related Decision Packet` list ending at DP-024) | `[확인 사실]` The committed contract text has **not** been edited to carry four of the five 2026-08-21 P1 decision packets that name it as their target. DP-029's Required changes: "§4 Snapshot should gain the tie-break and manifest-ordering rules... in its next revision." `[측정]` §4 as committed does not state a member-ordering or tie-break rule at all — it fixes materialization, dual-digest verification, and item-key uniqueness, and governs ordering only by reference to [DP-019](../decisions/DP-019-normalized-schema-0-1-and-results.md) D5 ("ordered by `(item_key)`," no collation fixed), which DP-029 D2/D3 amend without either amendment yet appearing in the contract's own text. DP-030's Required changes: §5 "should drop 'Determinism is required' as a blanket normalizer obligation" — §5 as committed still states it as an unqualified bullet, the position DP-030 D1 supersedes for P1. DP-033's Required changes: §8 "should gain the six-screen set and the Raw-read action... in its next revision" — §8 as committed still describes only the four P0-B operator actions. DP-034's Required changes: "none in `PoC Contract 0.1` directly; a future revision should add an explicit credential-entry clause under §8 Operations if the general operator-action language does not already cover it." This gate accepts the document as the record of what P0 measured and as the base P1 amends; it does not accept the document's text as already current, and a P1 implementer reading only this file would build §5's superseded determinism requirement and miss §4's and §8's amendments. The edit is Required-changes work each DP already assigns, unclaimed as of this revision — it is not this gate's file to touch. |
| P0 Artifact Disposition Register | `ACCEPTED (this gate, 2026-08-21)` | [`P0-ARTIFACT-DISPOSITION.md`](P0-ARTIFACT-DISPOSITION.md) (Status `DRAFT`; its own Decision block reads "Outcome: NOT ACCEPTED — pending the P1 Entry Gate") | The register names three blockers to its own acceptance; this gate closes them only partially. (1) The archive tag does not exist — carried into the P0 isolation checks below and left for Task 14. (2) `addon_host/`'s disposition is `UNRESOLVED` pending OQ-013 and OQ-014: OQ-014 is now `RESOLVED` ([DP-012](../decisions/DP-012-independent-scraper-services.md), narrowed for P1 by [DP-031](../decisions/DP-031-p1-collector-topology.md)), but OQ-013 clause C stays `OPEN` by the Reconstruction Plan's own 2026-08-21 decision not to resolve it — the disposition narrows, it does not close. (3) The register states "`SEC-006` must be satisfied, not waived, before P1 runs against real sources." `[결정]` DP-034 D3 does not satisfy `SEC-006`; it retires DP-023's waiver and replaces it with a separate, explicit, still-unimplemented deferral (`SR-005`). This gate treats that replacement as discharging the Reconstruction Plan Phase 0.3's alternative branch ("or record a P1-scoped decision that is not a waiver"), not as satisfying the register's stricter original wording — the two are different claims and should not be conflated. |
| P1 reconstruction plan | `ACCEPTED (this gate, 2026-08-21)` | [`P1-RECONSTRUCTION-PLAN.md`](P1-RECONSTRUCTION-PLAN.md) (Status `DRAFT_FOR_GATE`, updated 2026-08-21 with Phase 0 closure and the M1–M7 milestone table) | Phase 0 closure records item 0.2 (OQ-013 clause C) as deliberately not resolved, carried forward rather than closed, reopening only on a stated falsification condition met during M3/M4. The plan's own "Estimate discipline" states no durations, on the basis that P0's one recorded timebox was exceeded by ~24%; a reader should not infer a schedule from milestone ordering. Its "What P1 must not reproduce" section names "Prose claims about controls, unverified by any test" as the failure shape it judges least addressed by the project's toolchain and most likely to repeat — this gate record is itself exactly that kind of prose and claims no exemption from the risk it names. |
| Promoted acceptance and fixture inventory | `ACCEPTED (this gate, 2026-08-21)` | [`POC-CONTRACT-0.1.md`](../../contracts/experimental/POC-CONTRACT-0.1.md) §Acceptance criteria — `JOB-001…008`, `OPS-001…004`, `SEC-001…004`, 16 scenarios in [`tests/acceptance/`](../../tests/acceptance/), each with an executable counterpart, disposition `PROMOTE` in [`P0-ARTIFACT-DISPOSITION.md`](P0-ARTIFACT-DISPOSITION.md); [DP-022](../decisions/DP-022-structural-fixtures.md) structural-fixture principle, `tools/structural_fixture.py` (`RULESET_VERSION = "1"`), disposition `PROMOTE` | `[확인 사실]` `PoC Contract 0.1`'s own Acceptance criteria section warns that its `SEC-00N` are **scenario** ids, not the `SEC-00N` **requirement** ids in `p0-security.md` — the two numbering schemes collide by name, and citing one against the other misreports coverage; the contract's own mapping-table pointer must travel with them. `[확인 사실]` DP-022 D5 requires every structural fixture's manifest to state what it does *not* represent — a single capture cannot show a `429`, an empty result set it never returned, or a field the source omits only sometimes — so this inventory's promotion carries that representativeness gap forward rather than closing it. |
| Open Question and blocker inventory | `ACCEPTED (this gate, 2026-08-21)` | The annex below; [`P1-INHERITED-DEFECTS.md`](P1-INHERITED-DEFECTS.md); [`architecture-synthesis-v0.1.md`](architecture-synthesis-v0.1.md) §Unresolved blockers carried to the P1 Entry Gate | `[확인 사실]` `P1-INHERITED-DEFECTS.md` states plainly that it is not one of the artifacts this gate is defined to accept — [`README.md`](README.md) names four (Architecture Synthesis, disposition register, `PoC Contract 0.1`, reconstruction plan), matching this table's own row count, and whether the defects register becomes a fifth accepted artifact is the owner's decision, not asserted by this row. Accepting this row is accepting the inventory as accurately compiled from the sources above, not accepting `P1-INHERITED-DEFECTS.md` itself as a fifth gate artifact. `[확인 사실]` That section's item 4 — rate limiting, deep pagination, redirects, drift, and the `200`-with-an-error-body case, all unobserved against a real source (`B4-SCENARIO-COVERAGE.md:35`; `P1-INHERITED-DEFECTS.md` §8) — is carried open into M4, where the two live adapter targets (trend-radar, tubedepth) become the first real subjects able to exercise it; an earlier revision of this row cited the wrong document for this section and dropped item 4 from the inventory entirely. `[확인 사실]` At the *requirement* level (`p0-security.md:124-127`, a different numbering scheme from the `tests/acceptance/SEC-00N` scenarios row 10 below cites), requirement-ids `SEC-002`, `SEC-003`, and `SEC-004` have **no acceptance scenario at all** — carried here as an open blocker rather than closed by row 10's scenario-level citations. |

## P0 Charter exit-criteria annex

`[결정]` This annex answers `p0-charter.md`'s twelve P0-B exit criteria one by one, using the most current evidence rather than reproducing whichever prior document phrased a criterion first. Several passages across the documents the brief for this task names as sources are, on inspection, stale on this exact point — six in total; an earlier revision of this paragraph counted only two:

- `[확인 사실]` **Architecture Synthesis Part 6, row 1** (`:356`) still reads *"the dataset is a structural stand-in ([SRC-002])."* That was true only until [TASK-007](../agent-workflow/task-packets/TASK-007-obf-dataset-end-to-end.md) and [DP-027](../decisions/DP-027-dataset-standard-and-share-alike.md), both 2026-08-20. The same document's **Part 1, Question 2** was updated to describe the real Open Beauty Facts flow; Part 6's summary table was not. This annex uses the Part 1 / DP-027 text.
- `[확인 사실]` **`B4-SCENARIO-COVERAGE.md`** is dated 2026-08-19 and its "What B4 does not establish" §2 states *"No real dataset exists."* That is likewise superseded by the same 2026-08-20 work. Neither file is in this task's allowed scope to edit, so the correction is recorded here rather than in either source.
- `[확인 사실]` **Architecture Synthesis Part 6, row 7** (`:362`) still reads *"Replay across Raw-store evolution is unproved."* The same document's own **Part 1, Question 5** (`:149`) says the opposite as of [TASK-005](../agent-workflow/task-packets/TASK-005-snapshot-evolution-that-discriminates.md), 2026-08-20: *"It is exercised and it discriminates."* Row 7 is stale; this annex's row 7 below uses Question 5's narrower, exercised answer.
- `[확인 사실]` **Architecture Synthesis Part 2, row 3** (`:276`) still reads *"Validated for the shapes tested, with the dataset half self-authored."* The dataset half stopped being self-authored on the same 2026-08-20 date as the first two corrections above (TASK-007/DP-027); this row was not updated when Part 1 Question 2 was.
- `[확인 사실]` **Architecture Synthesis Part 2, row 4** (`:277`) still reads *"Half validated. Tampering detected; Raw-store evolution never exercised."* Superseded by the same TASK-005 work as row 7 above, and contradicted by the same document's own Part 1 Question 5.
- `[확인 사실]` **Architecture Synthesis Part 2, row 6** (`:279`, the rule-baseline row) still reads *"Not tested. No quality baseline was built; the normalizers extract, they do not judge."* `project-state.md` §5 records the opposite as of 2026-08-20: `normalizer.rule.baseline@0.1` was built and survived three independent attacks — on fixtures, not on real data, which is the distinction that row should carry and does not. This gate's own citation of `:279` (see the Architecture Synthesis row above) quotes it as it reads, not as current, for exactly this reason.

| # | Criterion | Status | Evidence | Limitation |
|---|---|---|---|---|
| 1 | One REST source and one dataset complete the end-to-end flow | **Met, with a limitation.** | `SRC-001` (NAVER, real); [DP-027](../decisions/DP-027-dataset-standard-and-share-alike.md) D2 — Open Beauty Facts delta exports pass through the installed importer into Raw, a sealed snapshot, and `normalizer.obf.product@0.1` at Schema 0.3, over the host path, since 2026-08-20. | `[측정]` Zero Korean sunscreen and zero Korean toner rows (DP-027 D2). The flow is real end to end; it carries **no product-relevant dataset evidence**, and DP-011's product scope moved to P1. |
| 2 | Source rights or permitted experimental use are recorded | **Met.** | `SRC-001` (redistribution explicitly `NO`); DP-027 D3 — ODbL §4.5(c): internal, unpublished use is outside §4.4, so no share-alike attaches while P0 publishes nothing built from OBF. | The obligation is inherited, not discharged: it attaches on P1's first publication from OBF-derived data. The attribution name itself is unsettled between "Open Food Facts" (terms page) and "Open Beauty Facts" (data page). [OQ-015](../open-questions/OQ-015-share-alike-data-class.md) stays `OPEN`. |
| 3 | Identical input replay demonstrates the chosen idempotency behavior | **Met.** | Unique index on `(run, item)` in `normalized_result`; `test_naver_real_data.py::TestTheNormalizerRunsOnRealData::test_normalizing_the_same_snapshot_twice_is_refused_rather_than_doubled`, against real captured data. | None named beyond general P0 scope. Architecture Synthesis Part 1 Question 3 calls this "P0's strongest result." |
| 4 | Changed source content creates a traceable new observation or version | **Partially met.** | Two captures of the same moving source produce two envelopes, two snapshots, two digests. | `[측정]` No test asserts what happens to the *overlap* between two captures of a moving source (`B4-SCENARIO-COVERAGE.md` §1). Not closed by any of DP-029…DP-034. |
| 5 | Parallel worker claims do not corrupt job state or create uncontrolled duplicate effects | **Met, with a limitation.** | `JOB-007`, 200 jobs × 4 processes; 0 failures in 30 runs after the B6 `job.claim_conflict` repair. | `[측정]` Load-sensitive at that scale — three standalone runs under CPU contention gave 1, 3, and 1 failures (`B4-SCENARIO-COVERAGE.md` §5). `[확인 사실]` [OQ-006](../open-questions/OQ-006-job-concurrency.md) F16: `test_job_002_shares_one_correlation_id_across_both_attempts` failed once under `pytest -n 4` and passed on re-run; cause unconfirmed. OQ-006 itself requires this be *"resolved or explicitly carried before the P1 Entry Gate."* This row is that carrying, not a resolution. |
| 6 | Collection and normalization failures can be recovered independently | **Met.** | [DP-019](../decisions/DP-019-normalized-schema-0-1-and-results.md) D6 — normalization is a separate, operator-started job; `test_normalizer_capability.py` — miscounting, orphaned lineage, and wrong-return-type failures are refused by name and leave `normalized_result` and its error summary in a failed state. `[확인 사실]` That Raw itself stays untouched is structural, not a test outcome (`B4-SCENARIO-COVERAGE.md:74` — "That is structural, not a test outcome"): the file asserts nothing against `raw_item`/`raw_envelope` directly; Raw's immunity follows from being sealed before normalization starts (DP-019 D6), a design property this test does not exercise. | None named beyond the structural point that Raw is sealed before normalization starts, which is a design property rather than a tested recovery mechanism. |
| 7 | A sealed snapshot replays the same exact normalizer input and detects tampering | **Met for tampering; narrow for evolution.** | `test_a_tampered_snapshot_fails_the_run_before_the_add_on_sees_a_byte` with an untampered control; TASK-005's four-step Raw-store-evolution discrimination (additive migration, superseding collection, purge) — the materialized design replays byte-identically at all four steps, a re-query design diverges at step three. | `[측정]` Against the *reference* design [OQ-004](../open-questions/OQ-004-snapshot-boundary.md) actually names (append-only rows fixed at seal), only the purge step separates the two — narrower than "evolution" read broadly. Two identity gaps were found: a same-key tie decided by the transaction timestamp `emitted_at`, whose `uuid4` fallback selected the *older* payload in 2 of 3 forced ties across 12 re-seals; and collation-dependent manifest ordering, so two clusters differing only in locale seal different digests from identical Raw. Repaired at the contract-decision level by DP-029 D2/D3, not yet implemented (M2). A purge does not discharge an erasure obligation — `snapshot_item` and `raw_envelope` still hold the bytes — routed undesigned to `SR-003` (DP-029 D4). |
| 8 | Different normalizer or schema versions can coexist for the same Raw lineage | **Met — over an empty lineage, and not falsifiably by this test's own "no UPDATE path" assertion.** | `TestVersionsCoexist` — two add-on versions and two output-contract versions over one snapshot, both readable, no UPDATE path. | `[확인 사실]` The "no UPDATE path" property this test reports is the same property [`P1-INHERITED-DEFECTS.md`](P1-INHERITED-DEFECTS.md) §3 F3 records as making the coexistence assertion unfalsifiable: there is no UPDATE path in the schema at all, so the assertion cannot go red for "a row updated in place" regardless of whether coexistence actually holds. `[확인 사실]` The criterion's "same Raw lineage" half is exercised over an **empty** lineage — `test_normalized_results.py:57-66`'s own fixture docstring: "An empty one is enough here — what these cases are about is the result rows." |
| 9 | The dashboard identifies what ran, its input, state, failure, and retry action without direct database inspection | **Met, with a limitation.** | `OPS-001`, `OPS-002`, `SEC-004` scenarios; `test_operator_loop.py` drives the four-act operator loop end to end, over a stub transport and a fixture credential — by the test's own module docstring: *"The transport is a stub, and the credential is a fixture... this one would pass against a source that does not exist."* The dashboard rendering real normalized rows is a separate, test-free observation: `B4-SCENARIO-COVERAGE.md:101` — "The dashboard rendered real normalized rows from the NAVER captures on 2026-08-19." | Tested only against P0-B's four-action operator surface. [DP-033](../decisions/DP-033-p1-operator-surface.md) widens P1's surface to six screens, including a reversed Raw-payload refusal and streaming exports, none of which is tested until M5. [OQ-005](../open-questions/OQ-005-operations-contract.md)'s telemetry-detail and diagnosis-scenario questions stay `OPEN` per DP-033. |
| 10 | Required platform and domain `SEC` scenarios pass within the declared local boundary | **Met for `SEC-001…004` as scenarios; `SEC-006` waived, not passed.** | `test_secret_store_guard.py` (`SEC-001`); `test_config.py` (`SEC-002`/`SEC-003`); `test_api.py` (`SEC-002`/`SEC-004`); `test_dashboard.py::test_sec_004_*` (`SEC-004`); [DP-023](../decisions/DP-023-sec-006-waived-for-p0.md) (`SEC-006` waiver). | `[결정]` DP-023's waiver states its own first obligation: "the P1 Entry Gate must not accept a plan that carries this forward." [DP-034](../decisions/DP-034-p1-credential-entry.md) D3 retires the waiver and replaces it with an independent, explicitly **unimplemented** decision (`SR-001`, `SR-004`, `SR-005`) — not with satisfaction. P1 starts real-source work against the same outbound-guard exposure that produced four defects in one day of P0-B adversarial review. `[확인 사실]` At the *requirement* level (`p0-security.md:124-127`, a different numbering scheme from the scenario ids this row's own Evidence column uses), requirement-ids `SEC-002`, `SEC-003`, and `SEC-004` have **no acceptance scenario at all** — the table there marks each "없음." This row's citations satisfy the scenario-numbering half of the coverage claim, not that requirement-level gap; the gap is also carried into the Open Question and blocker inventory row above. |
| 11 | Every Architecture Question is answered with evidence or an explicit unresolved blocker | **Met.** | Architecture Synthesis Part 1, all eight Architecture Questions. | "Answered" includes two refutations (Question 8's strong schema hypothesis, in its strong form) and one narrow result (Question 5's snapshot-evolution claim), not eight clean passes — see rows 1 and 7 above for the same findings, not restated as passes here. |
| 12 | The Architecture Synthesis, artifact disposition register, `PoC Contract 0.1`, and P1 reconstruction plan are accepted | **Pending this gate.** | Required outputs rows 2–5 above, each `ACCEPTED (this gate, 2026-08-21)`. | This criterion is discharged only by this gate's own acceptance and by nothing else. Until the owner sets an Outcome in the Decision block below, it remains unmet — the `PASS` recorded for the parent row treats criterion 12 as satisfied prospectively by this document's acceptance, not as pre-existing evidence. |

`[확인 사실]` One further status inconsistency, found while compiling the Open Question inventory below rather than the exit-criteria table, is recorded here because it bears on how much to trust a status field read in isolation: `OQ-009-credential-shape.md`'s own `- Status:` line still reads `OPEN`, and its `Resolution Decision Packet: not created` field is unfilled, while `project-state.md` §6 lists OQ-009 `RESOLVED` ("for P0-B by [DP-018] on one source's evidence; H1's query-parameter and signed-request cases stay open"). This gate follows `project-state.md` as the current record, per `AGENTS.md`'s rule that history documents and stale fields yield to the active project record, and does not count OQ-009 among the nine `OPEN` questions below — but the OQ file itself was never edited to say so, which a reader following only that file would miss.

## Open Question inventory

`[결정]` Verified by reading each Open Question file's own `- Status:` line, cross-checked against `project-state.md` §6, on 2026-08-21. Nine stay `OPEN`. (`OQ-004` was resolved by [DP-029](../decisions/DP-029-p1-snapshot-identity.md); `OQ-002`, `OQ-009`, `OQ-011`, `OQ-014` were already `RESOLVED` before this gate, `OQ-009`'s own file header notwithstanding — see the note above.)

| OQ | Question | How P1 carries it |
|---|---|---|
| [OQ-001](../open-questions/OQ-001-source-capability.md) | Which REST and dataset sources are usable and representative? | The REST half is closed for NAVER; the dataset *mechanism* is closed by DP-027/OBF, but DP-027 D2 records zero product-relevant rows. P1's Phase 4 (absorbed into M4) and [`RC-002`](../roadmap-candidates.md) (sunscreen/toner opportunity card) both require a still-unselected, product-relevant dataset source before DP-011's scope has real data to build on. |
| [OQ-003](../open-questions/OQ-003-normalization-protocol.md) | What are Schema 0.x and the normalizer provider protocol? | [DP-030](../decisions/DP-030-p1-normalization-scope.md) answers the five *scope* questions (determinism, per-record fault tolerance, quality judgment, schema inheritance, member order) but explicitly leaves the provider protocol itself (`NormalizeContext`, input/output/error contract) open, carried into the `addon_api` 1.3 reconstruction at M3. |
| [OQ-005](../open-questions/OQ-005-operations-contract.md) | Which platform and domain actions and evidence must the dashboard expose? | [DP-033](../decisions/DP-033-p1-operator-surface.md) D1/D3 fix the P1 screen set (six screens) and export shape; the telemetry-detail and diagnosis-scenario questions stay open, to be tested against M5/M6 implementation evidence rather than asserted here. |
| [OQ-006](../open-questions/OQ-006-job-concurrency.md) | Is the PostgreSQL job model sufficient under platform and domain failures? | No DP029–034 closes it. `JOB-007`'s load-sensitivity at 200×4 and OQ-006's own unconfirmed F16 parallel-test-execution flake — which OQ-006 itself requires "resolved or explicitly carried before the P1 Entry Gate" — are both carried into M1's `platform_core` rebuild for re-measurement, not assumed resolved because the shape survived P0-B. |
| [OQ-007](../open-questions/OQ-007-credential-scope.md) | What does the platform protect before a source exists, and which real credentials may a domain worker resolve? | [DP-034](../decisions/DP-034-p1-credential-entry.md) D1/D2 narrow the dashboard/API write-path half only (write-only credential entry; one bounded relaxation of `secret-setup.md` invariant 2). The worker-side scoping question — which `credential_ref` a worker may resolve at execution time — stays fully open per DP-034's own Remaining uncertainty, carried into M1/M2's domain rebuild. |
| [OQ-008](../open-questions/OQ-008-operator-reexecution-authority.md) | May an operator re-execute work that already succeeded, and what distinguishes that from retrying a failure? | DP-033 explicitly does not touch it. Its scheduler (D5) creates new `collect` jobs on a due schedule without re-executing succeeded work, which DP-033's own Remaining uncertainty says comes close to but does not settle the case; M6 is where DP-033 asks this to be checked once scheduling is real. |
| [OQ-010](../open-questions/OQ-010-cursor-stream-read-back.md) | Which cursor does an add-on read back when it writes several streams? | `PoC Contract 0.1`'s own limitation 4 states multi-stream cursors are "refused, not answered." No DP029–034 addresses it; it stays refused-by-rule into M3 (`addon_api`/`addon_host`) and becomes live only if a P1 adapter (trend-radar, tubedepth, DP-031 D3) needs more than one stream. |
| [OQ-013](../open-questions/OQ-013-addon-responsibility-boundary.md) | What is an add-on responsible for, and what holds a judgment nothing can check? (clause C) | The Reconstruction Plan's Phase 0 closure (2026-08-21) explicitly declines to resolve clause C now; `CONTRACT-ADDON-1.3.md`'s existing silence — "judgments only an add-on can make are checked by nothing" — is reconstructed as written. It reopens only if a falsification condition in OQ-013's own hypothesis table is met during M3/M4 implementation, not on a schedule. |
| [OQ-015](../open-questions/OQ-015-share-alike-data-class.md) | Where does share-alike-encumbered data sit in a three-class taxonomy? | Opened by DP-027 D4 because `public`/`local`/`private` has no slot for "redistributable only if you publish everything downstream." It stays open and unregistered until the first P1 artifact is published from a store holding an ODbL source — the OBF-derived `product` records Schema 0.3 already allows — which an `RC-002` opportunity card would trigger if adopted. |

## P0 isolation checks

- [x] No P0 module is planned as a P1 runtime or package dependency. `[결정]` P1 reuse is "rebuild against the contract; copy-and-adapt allowed; import forbidden" (reconstruction design spec §3, item 7); `P1-RECONSTRUCTION-PLAN.md`'s "What reconstruction means here" restates `AGENTS.md`'s rule, and every implementation row in [`P0-ARTIFACT-DISPOSITION.md`](P0-ARTIFACT-DISPOSITION.md) is `REBUILD_FROM_CONTRACT` or `ARCHIVE_REFERENCE_ONLY` — never `PROMOTE` for an importable module; only contracts, decisions, scenarios, and the generated-fixture tool promote as artifacts.
- [x] Every required P1 behavior links an accepted contract or explicit Open Question. `[확인 사실]` `P1-RECONSTRUCTION-PLAN.md`'s M1–M7 table names a contract, contract section, or Decision Packet for every milestone — M1 (DP-032), M2 (DP-029, DP-030), M3 (`CONTRACT-ADDON-1.3.md`, DP-008, OQ-013 carried open), M4 (DP-031, DP-030), M5 and M6 (DP-033), and M7 (the reconstruction spec §9, this gate's own record, DP-026's closure scope) — and its "What P1 must not reproduce" section routes every named P0 defect either to a contract-level repair (DP-029, DP-030) or to an explicit carried Open Question (OQ-013 clause C) rather than leaving any silently unrouted — see the Open Question inventory above for what "linked" does and does not mean per item. An earlier revision of this check asserted the same claim over a table where M3 named only module names and M7 named nothing; the table was repaired, not this check's wording alone.
- [x] P0 migrations, orchestration, temporary UI, and source-specific shortcuts are archive-only unless represented by a promoted contract or test. [`P0-ARTIFACT-DISPOSITION.md`](P0-ARTIFACT-DISPOSITION.md)'s Implementation table: `platform_core/`, `domain/`, `dashboard/` are `REBUILD_FROM_CONTRACT`; the three NAVER collectors, two NAVER normalizers, `normalizer.conformance`, `importer.local.jsonl`, `normalizer.obf.product`, `normalizer.rule.baseline`, and `addon_kit/` are all `ARCHIVE_REFERENCE_ONLY`.
- [x] Runtime Raw data, restricted downloads, temporary databases, caches, and protected logs have recorded retention or deletion responsibility. [`P0-ARTIFACT-DISPOSITION.md`](P0-ARTIFACT-DISPOSITION.md)'s data table assigns `DELETE_AFTER_EVIDENCE_CAPTURE` with **Operator** as the retention owner to the NAVER captures, local PostgreSQL databases, and `~/.config/cosmai/env`. The erasure-obligation gap this leaves for a *sealed* snapshot's own copies (`snapshot_item`, `raw_envelope`, which a `raw_item` purge does not reach) is separately routed to `SR-003` (DP-029 D4) as an explicitly undesigned P1 item, not asserted closed here.
- [x] P0 is ready to archive by Git tag or equivalent history. `[확인 사실]` Not yet created — the disposition register's own acceptance checklist lists the archive tag as its one unmet check, and `project-state.md` records the same gap. This gate proposes the tag name `p0-archive`, pointing at `00fdd0e` — the last commit that is pure P0 with no P1 decision work on it, **not** at the revision named in this document's header above. `[확인 사실]` Corrected 2026-08-21 (fix-wave round 2, N5): an earlier revision of this row called `00fdd0e` "`dev`'s state before this branch," which is imprecise — `dev` moved two commits past `00fdd0e` before this branch was cut: `2024fa7` (recording the P1 reconstruction design) and `2e7af88` (planning M0's fourteen tasks), neither of which is P0 work. `p1/entry-gate` was cut from `2e7af88`, `dev`'s tip at that point, not from `00fdd0e` directly. `[확인 사실]` `9547f3b` (this document's "Reviewed P0 revision") is a further, P1-decision commit on this unmerged branch: it carries DP-029 through DP-034, the six packets this gate is itself deciding whether to accept, so an archive tag placed there would not read as "P0, archived" to a later reader. `[측정]` `git diff --stat 00fdd0e dev -- experiments/ contracts/ tests/` is empty — the P0 tree itself (code, contracts, tests) has not moved across any of `00fdd0e`, `2024fa7`, `2e7af88`, or `9547f3b`; the revisions disagree only on which decision and planning documents sit on top of it, not on what P0 is. Creating the tag at `00fdd0e` is **Task 14's** action, consistent with `AGENTS.md`'s rule that commits, pushes, and tags happen when the owner asks. An earlier revision of this row proposed tagging the revision named in the header instead, which is the finding this correction repairs.

## Decision

- Outcome: `GO | CONDITIONAL GO | NO-GO`
- `[결정]`:
- Accepted conditions:
- Blocking failures:
- Failure classification:
- P0-B work package to reopen:

`GO` permits P1 reconstruction. `CONDITIONAL GO` may carry only explicit, bounded conditions that do not require P1 to infer a missing contract. `NO-GO` returns work to a named P0-B work package or records an external blocker.
