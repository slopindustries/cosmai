# Optional. Copy to .envrc and run `direnv allow` only if you want the Nix shell
# entered automatically. .envrc is ignored by Git so this never activates for
# anyone who did not ask for it.
use flake
