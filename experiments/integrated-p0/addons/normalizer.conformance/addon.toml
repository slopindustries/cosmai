# Generated by `addon_kit new normalizer.conformance --kind normalizer` from
# addon_kit/template/addon.toml. Replace the example fields below with what this
# add-on actually needs; see addon_kit/template/README.md for what each section
# means and what the normalizer kind may and may not declare (DP-008 D4).

[addon]
id = "normalizer.conformance"
version = "0.1.0"
kind = "normalizer"
entry = "handler:run"
requires_contract = ">=1.0,<2.0"
output_contract_version = "0.1"

[config]
schema_version = "1"

[[config.field]]
name = "strict"
type = "boolean"
required = false
label = "Strict parsing"
help = "Raise instead of skipping a snapshot item this add-on cannot parse"


