"""Skeleton entry point for the __KIND__ add-on __ADDON_ID__.

Generated by `addon_kit new __ADDON_ID__ --kind __KIND__` from
addon_kit/template/handler.py. Replace this docstring, the imports, and the body
of `run` with what this add-on actually does. The signature below is fixed by
DP-008 D4 for the __KIND__ kind — it is what `addon_host` checks at load time,
and changing it is changing what kind of add-on this is.

See addon_kit/template/README.md for what the __KIND__ kind is granted and refused.
"""

from __future__ import annotations

__IMPORTS__


def run(context: __CONTEXT_TYPE__) -> __OUTCOME_TYPE__:
    """Replace this docstring with what __ADDON_ID__ actually does."""
__BODY__
